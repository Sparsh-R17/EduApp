import 'package:flutter/material.dart';

class InstituteFilter extends StatelessWidget {
  const InstituteFilter({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      color: Colors.red,
      child: const Text('Institute Filteration'),
    );
  }
}
